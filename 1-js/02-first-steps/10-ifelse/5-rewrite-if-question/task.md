importance: 5

---

# 'if' in '?' umschreiben

Schreibe dieses `if` unter Verwendung des bedingten Operators `'?'` um:

```js
let result;

if (a + b < 4) {
  result = 'Darunter';
} else {
  result = 'Darüber';
}
```
